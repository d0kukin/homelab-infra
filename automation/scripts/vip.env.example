# =============================================================================
# vip.env.example — скопировать в vip.env и заполнить своими значениями
# vip.env добавлен в .gitignore и НЕ коммитится
# =============================================================================

# VIP — должен быть вне DHCP-пула роутера
VIP="192.168.31.X"
VIP_PREFIX="24"

# Wi-Fi интерфейс на нодах
INTERFACE="wlp2s0"

# Wi-Fi IP нод (зафиксировать DHCP-резервированием на роутере по MAC)
WIFI_IPS=("192.168.31.X" "192.168.31.X" "192.168.31.X")

# Management IP нод (vmbr0, для SSH)
MGMT_NODES=("10.10.10.2" "10.10.10.3" "10.10.10.4")

# VRRP приоритеты (убывающие, по одному на ноду)
PRIORITIES=(150 140 130)

# VRRP параметры
VRID="51"
AUTH_PASS="changeme"   # max 8 символов — keepalived молча обрезает длиннее
